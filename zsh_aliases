# macOS only
if [[ "$(uname -s)" = "Darwin" ]]; then
  alias of='open -a Finder ./'
fi

alias python='python3'
alias pip='pip3'
alias ...='../..'
alias ff='nvim "$(fzf)"'
alias hist='history | fzf'

# SSH
alias td='ssh tomd'
alias tm='ssh tomm'
